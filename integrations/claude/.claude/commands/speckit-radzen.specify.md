# Spec Kit Radzen — Specify

Use the `speckit-radzen` skill and execute the **specify** phase.

Follow `.speckit/radzen/core/workflows/specify.md` when that workflow file exists, plus the constitution and only the supporting standards/templates needed for this phase.

User context/arguments:

$ARGUMENTS
