# Spec Kit Radzen — Plan

Use the `speckit-radzen` skill and execute the **plan** phase.

Follow `.speckit/radzen/core/workflows/plan.md` when that workflow file exists, plus the constitution and only the supporting standards/templates needed for this phase.

User context/arguments:

$ARGUMENTS
