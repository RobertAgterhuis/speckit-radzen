# Spec Kit Radzen — Discover

Use the `speckit-radzen` skill and execute the **discover** phase.

Follow `.speckit/radzen/core/workflows/discover.md` when that workflow file exists, plus the constitution and only the supporting standards/templates needed for this phase.

User context/arguments:

$ARGUMENTS
