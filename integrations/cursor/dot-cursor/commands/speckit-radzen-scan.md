<!-- Generated by build/Build-Adapters.ps1 (Spec Kit Radzen 2.0.0). Do not edit; change core/agents/ instead. -->

# Spec Kit Radzen — Scan

Run the anti-pattern scanner on changed files and explain findings.


Run `pwsh .speckit/radzen/tools/speckit-radzen.ps1 scan -Feature <NNN>` (or `-All`). For each active finding explain the rule (`.speckit/radzen/core/antipatterns/`) and propose the fix. Never add a waiver without a reason the user approved.

No exit gate; continue with the next phase.

Report using the operating contract's reporting format.

Use the text the user typed after the command as context.
