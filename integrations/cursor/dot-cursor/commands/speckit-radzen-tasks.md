<!-- Generated by build/Build-Adapters.ps1 (Spec Kit Radzen 2.0.0). Do not edit; change core/agents/ instead. -->

# Spec Kit Radzen — Tasks

Break slices into traceable tasks in tasks.md.

Follow `.speckit/radzen/core/workflows/05-tasks.md` exactly, together with the constitution (`.speckit/radzen/core/constitution/constitution.md`) and the operating contract (`.speckit/radzen/core/agents/operating-contract.md`).

Act in the **planner** role (`.speckit/radzen/core/agents/roles/planner.md`).
No exit gate; continue with the next phase.

Report using the operating contract's reporting format.

Use the text the user typed after the command as context.
