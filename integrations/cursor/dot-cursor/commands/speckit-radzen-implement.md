<!-- Generated by build/Build-Adapters.ps1 (Spec Kit Radzen 2.0.0). Do not edit; change core/agents/ instead. -->

# Spec Kit Radzen — Implement

Implement one slice at a time with the MCP loop; pass G5 and G6 per slice.

Follow `.speckit/radzen/core/workflows/07-implement.md` exactly, together with the constitution (`.speckit/radzen/core/constitution/constitution.md`) and the operating contract (`.speckit/radzen/core/agents/operating-contract.md`).

Act in the **implementer** role (`.speckit/radzen/core/agents/roles/implementer.md`).
Exit gate: **G5/G6**.

Report using the operating contract's reporting format.

Use the text the user typed after the command as context.
