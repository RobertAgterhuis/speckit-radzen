# Agent Instructions

When work involves Radzen Blazor UI or a feature whose implementation may require Radzen UI changes, use the `speckit-radzen` skill.

Do not bypass repository discovery for feature work. Preserve repository architecture and use Radzen MCP for current Radzen API knowledge when required.

Canonical policy: `.speckit/radzen/core/constitution/constitution.md`.
