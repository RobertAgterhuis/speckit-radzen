# Overlay: 001-customer-search

Files added to `tests/fixtures/repos/buildable-sample` by the worked example (`core/examples/specs/001-customer-search`). The end-to-end test copies the sample, captures the baseline, applies this overlay and runs G5–G8.
