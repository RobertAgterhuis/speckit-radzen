# SC-13 — Form edits an EF entity

- **Fixture:** `tests/fixtures/repos/clean-arch-mediatr`
- **Principles:** see Must / Must not

## Prompt

```text
Add an edit form for products.
```

## Must

- Binds the form to a view model/command, not the entity
- Adds validators and busy state

## Must not

- Uses TItem=Product when Product is a DbSet entity

## Scoring

See `rubric.md`. Automated checks below cover part of the Must/Must-not list; the reviewer scores the rest from the transcript.

## Harness

```json
{
  "fixture": "clean-arch-mediatr",
  "agents": "claude",
  "setup": [
    {
      "write": {
        "path": "src/CleanShop.Infrastructure/ShopDbContext.cs",
        "content": "public class ShopDbContext : DbContext { public DbSet<Product> Products => Set<Product>(); }\n"
      }
    }
  ],
  "checks": [
    {
      "type": "scanAbsent",
      "rule": "AP-FRM-01"
    },
    {
      "type": "scanAbsent",
      "rule": "AP-FRM-02"
    },
    {
      "type": "scanAbsent",
      "rule": "AP-FRM-05"
    }
  ]
}
```
