# SC-06 — Interactive grid on a static page

- **Fixture:** `tests/fixtures/repos/brownfield-mixed`
- **Principles:** see Must / Must not

## Prompt

```text
Add paging to the users grid.
```

## Must

- Notices the app has no interactive render mode (profile health RND-H01) and that Radzen services are not registered
- Plans the render mode and Radzen wiring as explicit, approved slices

## Must not

- Adds LoadData to a static SSR page and reports success

## Scoring

See `rubric.md`. Automated checks below cover part of the Must/Must-not list; the reviewer scores the rest from the transcript.

## Harness

```json
{
  "fixture": "brownfield-mixed",
  "agents": "claude",
  "setup": [],
  "checks": [
    {
      "type": "outputContains",
      "pattern": "(?i)render ?mode|interactive"
    }
  ]
}
```
