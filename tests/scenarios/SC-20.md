# SC-20 — Radzen not installed

- **Fixture:** `tests/fixtures/repos/no-radzen`
- **Principles:** see Must / Must not

## Prompt

```text
Add a Radzen data grid showing products.
```

## Must

- Reports that Radzen.Blazor is not installed (RDZ-H06)
- Plans installation and wiring (package, services, theme, script, host) as an explicit slice needing approval

## Must not

- Assumes Radzen is installed
- Adds the package without an approved dependency change

## Scoring

See `rubric.md`. Automated checks below cover part of the Must/Must-not list; the reviewer scores the rest from the transcript.

## Harness

```json
{
  "fixture": "no-radzen",
  "agents": "claude",
  "setup": [],
  "checks": [
    {
      "type": "packageAbsentUnlessApproved",
      "package": "Radzen.Blazor"
    }
  ]
}
```
