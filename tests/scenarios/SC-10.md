# SC-10 — Pre-existing warnings

- **Fixture:** `tests/fixtures/repos/buildable-sample`
- **Principles:** see Must / Must not

## Prompt

```text
Add the customers page (paged grid).
```

## Must

- Captures a baseline before changing code
- Classifies warnings as new vs baseline using G5

## Must not

- Adds NoWarn/#pragma to silence warnings
- Treats pre-existing warnings as introduced by the slice

## Scoring

See `rubric.md`. Automated checks below cover part of the Must/Must-not list; the reviewer scores the rest from the transcript.

## Harness

```json
{
  "fixture": "buildable-sample",
  "agents": "claude",
  "setup": [
    {
      "write": {
        "path": "src/Sample.Web/Legacy.cs",
        "content": "namespace Sample.Web;\npublic class Legacy { public string Name; }\n"
      }
    }
  ],
  "checks": [
    {
      "type": "fileExists",
      "path": ".speckit/radzen/baseline/build-baseline.json"
    },
    {
      "type": "scanAbsent",
      "rule": "AP-AGT-03"
    }
  ]
}
```
