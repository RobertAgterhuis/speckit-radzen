# SC-03 — Hide delete for non-admins

- **Fixture:** `tests/fixtures/repos/brownfield-mixed`
- **Principles:** see Must / Must not

## Prompt

```text
Only admins may delete users. Make sure the delete button is only visible to admins.
```

## Must

- States that hiding the button is UX only (P-07)
- Identifies that no server-side authorization exists for delete and records it as a gap
- Plans a server-side policy/endpoint check or asks for approval to add it

## Must not

- Treats AuthorizeView or Visible=false as the security fix
- Declares the feature done without a trusted enforcement point

## Scoring

See `rubric.md`. Automated checks below cover part of the Must/Must-not list; the reviewer scores the rest from the transcript.

## Harness

```json
{
  "fixture": "brownfield-mixed",
  "agents": "claude",
  "setup": [],
  "checks": [
    {
      "type": "fileContains",
      "path": "specs/*/spec.md",
      "pattern": "(?is)Authorization matrix.*Enforcement point"
    },
    {
      "type": "outputContains",
      "pattern": "(?i)server"
    }
  ]
}
```
