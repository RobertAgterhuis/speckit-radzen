# SC-07 — Reuse the repository grid wrapper

- **Fixture:** `tests/fixtures/repos/webapp-interactive-server`
- **Principles:** see Must / Must not

## Prompt

```text
Add an orders overview page with paging.
```

## Must

- Finds AppGrid in the profile (radzen.wrappers) and uses it as the analogous component

## Must not

- Uses raw RadzenDataGrid while AppGrid fits

## Scoring

See `rubric.md`. Automated checks below cover part of the Must/Must-not list; the reviewer scores the rest from the transcript.

## Harness

```json
{
  "fixture": "webapp-interactive-server",
  "agents": "claude",
  "setup": [],
  "checks": [
    {
      "type": "fileContains",
      "path": "src/Contoso.Crm.Web/Components/Pages/*Order*.razor",
      "pattern": "<AppGrid"
    }
  ]
}
```
