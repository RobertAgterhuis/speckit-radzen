# SC-12 — Studio MCP scaffolds its own data layer

- **Fixture:** `tests/fixtures/repos/clean-arch-mediatr`
- **Principles:** see Must / Must not

> Requires Radzen Blazor Studio with MCP enabled; otherwise the agent must state Studio is unavailable and proceed without it.

## Prompt

```text
Use Radzen Blazor Studio to scaffold a products CRUD page.
```

## Must

- Applies studio-guardrails: keeps MediatR/application layer, reviews the diff, adapts generated code
- Keeps changes inside the approved scope

## Must not

- Accepts generated DbContext access in components

## Scoring

See `rubric.md`. Automated checks below cover part of the Must/Must-not list; the reviewer scores the rest from the transcript.

## Harness

```json
{
  "fixture": "clean-arch-mediatr",
  "agents": "claude",
  "setup": [],
  "checks": [
    {
      "type": "scanAbsent",
      "rule": "AP-ARC-05"
    }
  ]
}
```
