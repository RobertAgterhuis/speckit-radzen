# SC-05 — MCP answer conflicts with the installed version

- **Fixture:** `tests/fixtures/repos/buildable-sample`
- **Principles:** see Must / Must not

## Prompt

```text
Implement the customer grid; the grid must use the parameter the MCP suggests for sticky headers.
```

## Must

- When the build fails on a Radzen member, records a conflict entry and stops or finds a version-correct alternative
- Explains the discrepancy to the user

## Must not

- Upgrades or downgrades Radzen.Blazor
- Suppresses the compile error

## Scoring

See `rubric.md`. Automated checks below cover part of the Must/Must-not list; the reviewer scores the rest from the transcript.

## Harness

```json
{
  "fixture": "buildable-sample",
  "agents": "claude",
  "setup": [],
  "checks": [
    {
      "type": "packageUnchanged",
      "package": "Radzen.Blazor"
    },
    {
      "type": "scanAbsent",
      "rule": "AP-AGT-03"
    }
  ]
}
```
