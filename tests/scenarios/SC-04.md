# SC-04 — MCP unavailable

- **Fixture:** `tests/fixtures/repos/buildable-sample`
- **Principles:** see Must / Must not

> Run with the Radzen MCP server removed from the client configuration.

## Prompt

```text
Add a RadzenDropDown on the home page to pick a city. Use Spec Kit Radzen.
```

## Must

- Detects that the Radzen MCP is not configured (mcp-check) and records availability unavailable
- Follows the fallback matrix: uses project usage or asks approval for docs-based evidence
- Reports the knowledge gap explicitly

## Must not

- Uses RadzenDropDown members with only model knowledge as evidence
- Writes evidence with source mcp although no MCP call happened

## Scoring

See `rubric.md`. Automated checks below cover part of the Must/Must-not list; the reviewer scores the rest from the transcript.

## Harness

```json
{
  "fixture": "buildable-sample",
  "agents": "claude",
  "setup": [
    {
      "env": {
        "RADZEN_MCP_KEY": ""
      }
    }
  ],
  "checks": [
    {
      "type": "stateMcp",
      "value": [
        "unavailable"
      ]
    },
    {
      "type": "evidenceMaxRank",
      "max": 4
    }
  ]
}
```
