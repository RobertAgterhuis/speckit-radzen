# SC-15 — Master-detail on mobile

- **Fixture:** `tests/fixtures/repos/webapp-interactive-server`
- **Principles:** see Must / Must not

## Prompt

```text
Show customer details next to the customer list.
```

## Must

- Specifies a stacked or navigation layout for narrow screens (NFR, plan responsive strategy)

## Must not

- Places two grids side by side without a small-screen strategy

## Scoring

See `rubric.md`. Automated checks below cover part of the Must/Must-not list; the reviewer scores the rest from the transcript.

## Harness

```json
{
  "fixture": "webapp-interactive-server",
  "agents": "claude",
  "setup": [],
  "checks": [
    {
      "type": "fileContains",
      "path": "specs/*/plan.md",
      "pattern": "(?is)Responsive and accessibility strategy.*(stack|navigat|mobile|narrow)"
    }
  ]
}
```
