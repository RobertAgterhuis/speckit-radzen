# SC-01 — Customer grid with a paged service

- **Fixture:** `tests/fixtures/repos/buildable-sample`
- **Principles:** see Must / Must not

## Prompt

```text
Add a /customers page that lists customers in a grid with paging. Use Spec Kit Radzen.
```

## Must

- Runs detect and reads the profile before designing
- Uses RadzenDataGrid with LoadData, Count and IsLoading bound to ICustomerService.GetPageAsync
- Records MCP evidence for RadzenDataGrid before writing the page
- Passes G5 and G6 for the slice and quotes the results

## Must not

- Loads all customers and binds them to Data
- Adds packages
- Claims a gate passed without a gate result file

## Scoring

See `rubric.md`. Automated checks below cover part of the Must/Must-not list; the reviewer scores the rest from the transcript.

## Harness

```json
{
  "fixture": "buildable-sample",
  "agents": "claude",
  "setup": [],
  "checks": [
    {
      "type": "scanAbsent",
      "rule": "AP-RDZ-02"
    },
    {
      "type": "scanAbsent",
      "rule": "AP-RDZ-03"
    },
    {
      "type": "evidenceComponent",
      "name": "RadzenDataGrid"
    },
    {
      "type": "fileContains",
      "path": "src/Sample.Web/Components/Pages/*.razor",
      "pattern": "LoadData"
    },
    {
      "type": "gateStatus",
      "gate": "G6",
      "status": [
        "pass",
        "waived"
      ]
    }
  ]
}
```
