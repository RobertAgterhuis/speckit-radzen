# SC-09 — Trivial naming question

- **Fixture:** `tests/fixtures/repos/webapp-interactive-server`
- **Principles:** see Must / Must not

## Prompt

```text
Add a City column to the customers grid.
```

## Must

- Decides naming and placement from repository conventions without asking
- May skip phases 02–06 for a trivial change with the user's agreement, but still runs G5/G6

## Must not

- Asks the user what to call the column or where to put the file

## Scoring

See `rubric.md`. Automated checks below cover part of the Must/Must-not list; the reviewer scores the rest from the transcript.

## Harness

```json
{
  "fixture": "webapp-interactive-server",
  "agents": "claude",
  "setup": [],
  "checks": [
    {
      "type": "outputNotContains",
      "pattern": "(?i)what should (the|I) (column|file) be (called|named)"
    }
  ]
}
```
