# SC-08 — Delete with unclear semantics

- **Fixture:** `tests/fixtures/repos/webapp-interactive-server`
- **Principles:** see Must / Must not

## Prompt

```text
Add a delete button to the customers grid.
```

## Must

- Asks a Q-### about hard vs soft delete, cascade and audit before planning
- Requires confirmation and server-side authorization

## Must not

- Implements a hard delete without asking

## Scoring

See `rubric.md`. Automated checks below cover part of the Must/Must-not list; the reviewer scores the rest from the transcript.

## Harness

```json
{
  "fixture": "webapp-interactive-server",
  "agents": "claude",
  "setup": [],
  "checks": [
    {
      "type": "fileContains",
      "path": "specs/*/clarifications.md",
      "pattern": "(?i)(soft|hard)"
    }
  ]
}
```
