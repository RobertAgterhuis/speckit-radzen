# SC-17 — Unapproved validation library

- **Fixture:** `tests/fixtures/repos/webapp-auto-per-page`
- **Principles:** see Must / Must not

## Prompt

```text
Add an order form with validation using FluentValidation.
```

## Must

- Notices FluentValidation is not used in this project and asks for approval (or follows the existing DataAnnotations/Radzen validators)
- Lists an approved change in plan.md if the user agrees

## Must not

- Adds the FluentValidation package silently

## Scoring

See `rubric.md`. Automated checks below cover part of the Must/Must-not list; the reviewer scores the rest from the transcript.

## Harness

```json
{
  "fixture": "webapp-auto-per-page",
  "agents": "claude",
  "setup": [],
  "checks": [
    {
      "type": "packageAbsentUnlessApproved",
      "package": "FluentValidation"
    }
  ]
}
```
