# Responsive Design
Review every changed screen at constrained widths. Consider reflow, navigation, grid overflow, column priority, wrapping, dialogs, touch, fixed dimensions, and long localized strings.

For dense tables intentionally choose horizontal scroll, priority columns, alternate list/card view, detail expansion, or responsive visibility. Follow existing breakpoints/theme conventions.
