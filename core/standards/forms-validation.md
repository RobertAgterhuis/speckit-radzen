# Forms and Validation
Follow the repository's established form/validation model. Client validation improves UX but never replaces trusted business validation.

Provide actionable validation, preserve typed/nullability semantics, handle submit/busy/success/failure intentionally, and prevent accidental duplicate submission when relevant.

Use the existing page/dialog/inline-editing pattern. Treat destructive actions deliberately and keep authorization server-enforced.
