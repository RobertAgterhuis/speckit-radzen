# Component Selection
Choose using interaction, local patterns, cardinality, editing model, responsiveness, accessibility, performance, and maintainability.

Reuse order: existing project component/pattern → suitable Radzen component → supported composition → justified custom component.

Do not use DataGrid for every collection or create wrappers that add no stable project value.
