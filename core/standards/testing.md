# Testing
Discover and use the repository's existing test stack. Do not add a framework simply because it is common.

Test at the boundary matching changed behavior: application/domain, endpoint/integration, component, authorization, or E2E.

For Radzen UI prefer behavior-oriented tests over brittle markup snapshots unless snapshots are already a repository convention.
