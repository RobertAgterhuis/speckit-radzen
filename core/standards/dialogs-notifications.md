# Dialogs and Notifications
Use dialogs for bounded contextual interactions, not long complex workflows. Consider title/context, focus, keyboard behavior, validation, busy state, cancellation, destructive confirmation, and mobile viewport.

Use the repository's notification mechanism. Do not expose secrets/internal exceptions. Notifications are not a replacement for field validation.
