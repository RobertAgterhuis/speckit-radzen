# Performance
Evaluate request count, payload size, cardinality, rendering cost, repeated renders, expensive templates, cancellation, debounce, and server query support.

Do not optimize speculatively. Full client materialization of unbounded data requires explicit justification.
