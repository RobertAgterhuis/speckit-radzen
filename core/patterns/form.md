# Pattern: Form
Specify model/contract, create/edit mode, required fields, validation source, submit operation, authorization, cancellation, success/failure behavior, and concurrency if relevant.

Reuse existing form abstractions. Do not bind persistence entities directly when the repository uses contracts/view models.
