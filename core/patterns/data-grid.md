# Pattern: Data Grid
Use when users need structured comparison plus sorting/filtering/paging/selection/row actions/editing.

Specify columns, identity, data source, load strategy, sort/filter boundary, paging, row actions, authorization, responsive behavior, and UI states.

Verify exact RadzenDataGrid APIs through MCP before implementation.
