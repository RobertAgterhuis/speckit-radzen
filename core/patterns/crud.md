# Pattern: CRUD
Do not assume a management screen requires all CRUD operations. Discover Read/Create/Update/Delete, authorization, validation, and tests independently.

Choose page/dialog/inline editing according to local patterns and complexity. For delete, determine hard/soft semantics, authorization, dependencies, confirmation, and failure behavior.
