# Pattern: Dashboard
Specify metric definitions, freshness, time range, authorization, per-widget loading/failure, responsive stacking, drill-down, and no-data semantics.

Do not load large raw datasets solely to aggregate in the browser when the trusted/backend layer can aggregate.
