# Phase 09 — Done

**Purpose:** close the feature with verifiable evidence.

## Entry — G7 passed

## Steps

1. Complete `checklists/definition-of-done.md` in `review.md`.
2. Run `speckit-radzen gate G8 -Feature NNN`. It re-checks that G0–G7 passed after the last code change and renders `gate-report.md`.
3. Summarize for the user: what changed (files), how each AC is verified, gate results, open follow-ups (out-of-scope findings from discovery), and any waivers.

## Exit — G8

`state.json` phase is `done`; `gate-report.md` is up to date.
